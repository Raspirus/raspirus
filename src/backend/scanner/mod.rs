/// Holds the file indexer
mod index;
/// Holds the file logger responsible for keeping track of scan results
pub mod log;
/// Holds the scanner struct and its functionality
pub mod scanner;
/// Holds the structs needed for various functions of the file scanner
mod structs;
