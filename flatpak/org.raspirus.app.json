{
    "app-id": "org.raspirus.app",
    "runtime": "org.freedesktop.Platform",
    "runtime-version": "24.08",
    "sdk": "org.freedesktop.Sdk",
    "sdk-extensions": ["org.freedesktop.Sdk.Extension.rust-stable"],
    "command": "raspirus",
    "finish-args": [
        "--share=ipc",
        "--socket=fallback-x11",
        "--socket=wayland",
        "--share=network",
        "--filesystem=home",
        "--device=dri"
    ],
    "build-options": {
        "append-path": "/usr/lib/sdk/rust-stable/bin"
    },
    "modules": [{
        "name": "raspirus",
        "buildsystem": "simple",
        "build-options": {
            "env": {
                "CARGO_HOME": "/run/build/raspirus/cargo"
            }
        },
        "build-commands": [
            "cargo fetch --manifest-path Cargo.toml --verbose",
            "cargo build --release --verbose",
            "install -Dm755 ./target/release/raspirus -t /app/bin/",
            "install -Dm644 ./flatpak/${FLATPAK_ID}.metainfo.xml -t /app/share/metainfo/",
            "install -Dm644 ./flatpak/${FLATPAK_ID}.desktop -t /app/share/applications/",
            "install -Dm644 ./flatpak/${FLATPAK_ID}.svg -t /app/share/icons/hicolor/scalable/apps/"
        ],
        "sources": [{
                "type": "archive",
                "url": "https://github.com/Raspirus/raspirus/archive/refs/tags/v2.1.0.tar.gz",
                "sha256": "518c3a52914cf63787bf48ff4fd1798ff0e894aa26bf14a244e44a2d34b9d6e8"
            }
        ]
    }]
}
